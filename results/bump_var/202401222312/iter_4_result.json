{
  "time": {
    "17": 98,
    "18": 98,
    "20": 98
  },
  "prompt": {
    "17": "else if (conflicts >= 10000 && fast_lbd_sum / lbd_queue_size > 25) restart();",
    "18": "else if (conflicts >= 10000 && fast_lbd_sum / lbd_queue_size > 100) restart();",
    "20": "else if (conflicts >= 10000 && fast_lbd_sum > lbd_queue_size * 100) restart();"
  }
}